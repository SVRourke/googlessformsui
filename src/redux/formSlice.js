import { createSlice } from "@reduxjs/toolkit";

const initialState = []

export const formSlice = createSlice({
    name: 'forms',
    initialState,
    reducers: {
        // load
        // add
        // delete
        // clear
    }
})